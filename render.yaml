services:
  - type: web
    name: lms-app
    env: static
    buildCommand: cd frontend && npm run build
    staticPublishPath: ./frontend/build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
